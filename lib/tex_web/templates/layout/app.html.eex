<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Tex · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <input type="checkbox" id="check">
    <label for="check" id="btn"><i class="fa fa-bars text-white"></i></label>
    <label for="check" id="cancel"><i class="fa fa-times text-white"></i></label>
    <h1 class="text-center mt-3 font-weight-bold">тєχ</h1>
    <div class="sidebar">
        <div class="side-container">
          <img src="<%= Routes.static_path(@conn, Tex.ImageUploader.url({Tex.Accounts.get_user!(@current_user.id).account.image_path, []}))%>">
          <h5 class="text-white mt-3">Hatsor</h5>
        </div>
        <ul>
            <li><%=link "Add post", to: Routes.post_path(@conn, :new), class: "a"%></li>
            <li><%=link "Home", to: Routes.post_path(@conn, :index), class: "a"%></li>
        </ul>
    </div>
    <main role="main">
      <%= if get_flash(@conn, :info) do %>
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <% end %>
      <%= if get_flash(@conn, :error) do %>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <% end %>
      <%= @inner_content %>
      <i class="fa fa-check"></i>
      <i class="fa fa-eye"></i>
      <i class="fa fa-check fa-lg"></i>
    </main>
  </body>
</html>
